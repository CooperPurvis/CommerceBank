server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/commerce_database
    username: commerceuser
    password: CommerceBank1!
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: true
  application:
    name: Commerce-Bank